import { HeaderLogo } from '../assets/images';

const Nav = () => {
  return (
    <header>
      <nav>
        <img src={HeaderLogo} alt='' />
        NIle
      </nav>
    </header>
  );
};

export default Nav;
